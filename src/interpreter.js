import React from 'react';

const interpreter = (array) => {
  // switch statement with the different components

  const buildScreen = () => {
    const screen = array.map((c) => {
      // build the correct component based on type
    });
  };

  return <div>interpreter</div>;
};

export default interpreter;
